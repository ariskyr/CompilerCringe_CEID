PROGRAM test

    FUNCTION             foo(INTEGER a, CHAR _b1)
        IF(a>int) THEN
            int = a ;
        ELSE
            kati = "mikroteros";
        ENDIF
        RETURN int
    END_FUNCTION

    STARTMAIN

        VARS CHAR x[5], y;
        VARS INTEGER z;
        z = foo();
        z = foo(10, bb);
        PRINT("auto einai to apotelesma:" [,z])
    ENDMAIN
    