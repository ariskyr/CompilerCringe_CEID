PROGRAM test

    FUNCTION foo(INTEGER a, CHAR _b1)
        VARS CHAR kati = "kati";
        VARS INTEGER int = 5;
        IF(a>int) THEN
            int = a;
        ELSE
            kati = "mikroteros";
        ENDIF
        RETURN int;
    END_FUNCTION

    STARTMAIN

        VARS CHAR x[5], y;
        VARS INT z;
        z = foo(10, "bb");
    ENDMAIN
    